# 参数对比：代码 vs 论文

## 参数分类

### ✅ 论文参数（表2-1或公式中明确给出）

| 参数 | 代码值 | 论文值 | 来源 | 状态 |
|------|--------|--------|------|------|
| `rho_air` | 1.225 kg/m³ | 1.225 kg/m³ | 标准大气密度 | ✅ 一致 |
| `A_rotor` | 0.503 m² | 0.503 m² | 论文公式(2-8) | ✅ 一致 |
| `Omega` | 300 rad/s | 300 rad/s | 论文表2-1 | ✅ 一致 |
| `R_rotor` | 0.4 m | 0.4 m | 论文表2-1 | ✅ 一致 |
| `delta_drag` | 0.012 | 0.012 | 论文表2-1 | ✅ 一致 |
| `kappa` | 1.1 | 1.1 | 论文表2-1 | ✅ 一致 |
| `S_FP` | 0.0151 m² | 0.0151 m² | 论文表2-1 | ✅ 一致 |
| `g` | 9.8 m/s² | 9.8 m/s² | 标准重力加速度 | ✅ 一致 |

### ⚠️ 论文参数（需要验证）

| 参数 | 代码值 | 论文值 | 来源 | 状态 |
|------|--------|--------|------|------|
| `b_blades` | 4 | ? | 假设（4旋翼） | ⚠️ 需验证 |
| `c_chord` | 0.0157 m | ? | 假设（用于计算桨盘实度） | ⚠️ 需验证 |
| `s_solidity` | 计算值 | ? | 计算：`b*c*R/(π*R²)` | ⚠️ 需验证 |

### ❌ 用户自定义参数（不在论文中）

| 参数 | 代码值 | 说明 | 状态 |
|------|--------|------|------|
| `UAV_m` | 2 kg | 无人机自身重量 | ❌ 用户定义 |
| `demand_q` | [0.5, 0.7, ...] kg | 载重（10个客户点） | ❌ 用户定义 |
| `v_h` | 10 m/s | 水平飞行速度 | ❌ 用户定义 |
| `v_u` | 5 m/s | 垂直上升速度 | ❌ 用户定义 |
| `v_d` | 3 m/s | 垂直下降速度 | ❌ 用户定义 |
| `h_up_down` | 40 m | 垂直高度 | ❌ 用户定义 |

### 📝 计算得到的参数

| 参数 | 计算公式 | 状态 |
|------|----------|------|
| `P_0` | `(delta_drag/8) * rho_air * s_solidity * A_rotor * Omega^3 * R_rotor^3` | ✅ 按论文公式计算 |
| `v_0` | `sqrt((UAV_mass * g) / (2 * rho_air * A_rotor))` | ✅ 按论文公式(2-8)计算 |
| `mu` | `v_h / (Omega * R_rotor)` | ✅ 按论文公式计算 |
| `s_solidity` | `b_blades * c_chord * R_rotor / (pi * R_rotor^2)` | ⚠️ 需要验证公式 |

## 关键发现

### ✅ 已正确实现的论文参数

1. **物理常数**：
   - `rho_air = 1.225` ✅
   - `g = 9.8` ✅

2. **论文表2-1参数**：
   - `Omega = 300` ✅
   - `R_rotor = 0.4` ✅
   - `delta_drag = 0.012` ✅
   - `kappa = 1.1` ✅
   - `S_FP = 0.0151` ✅
   - `A_rotor = 0.503` ✅

3. **公式计算**：
   - `v_0` 计算 ✅（按公式2-8）
   - `P_0` 计算 ✅（按公式2-30）
   - `mu` 计算 ✅（前进比）
   - 水平功率公式 ✅（按公式2-38）

### ⚠️ 需要验证的参数

1. **桨盘实度相关**：
   - `b_blades = 4`（旋翼数量）- 需要确认论文中是否指定
   - `c_chord = 0.0157`（翼型弦长）- 需要确认论文中是否给出
   - `s_solidity` 计算公式 - 需要确认是否正确

### ❌ 用户自定义参数（合理）

这些参数是用户根据具体应用场景定义的，不在论文中：
- 无人机重量、载重、飞行速度、高度等

## 结论

**除了用户自定义的参数（速度、载重、高度）外，代码中的其他参数都与论文一致。**

**唯一需要验证的是：**
- 桨盘实度 `s` 的计算公式和参数值（`b_blades`, `c_chord`）是否与论文一致

## 建议

1. **检查论文中是否有关于桨盘实度的说明**：
   - 旋翼数量 `b`
   - 翼型弦长 `c`
   - 桨盘实度 `s` 的计算公式

2. **如果论文中没有明确给出**：
   - 当前的计算方法（`s = bcR/(πR²)`）是标准的桨盘实度定义
   - 参数值（`b=4`, `c=0.0157`）可能需要根据实际无人机型号调整
